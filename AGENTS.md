# 公開用リポジトリの運用ルール

- このリポジトリはOpenConceptの公開用フォルダを管理する。交換用の最新の公開配布物のみを置き、開発途中のファイル、作業用ファイル、旧版の保管場所にはしない。
- 編集・更新済みの配布物の取得元は、OpenConceptプロジェクト内の`Dist`フォルダ以下とする。この環境でのパスは`F:\AI\Antigravity\OpenConcept\Dist`。
- 更新は手動、またはユーザーの更新指示に応じて行う。自動同期や、指示のないバージョンへの入れ替えは行わない。バージョンの指定がある場合は、そのバージョンの配布物を取得する。
- 配布物は`Dist`から取得してこのリポジトリに配置する。ここで配布物を直接編集して、取得元と異なる修正版を作らない。
- 配置前に対象のバージョンと配布物一式を確認する。ZIP、展開済みファイル、manifest、チェックサムがある場合は、同じ配布物として整合していることを確認する。
- 本体は`core/<version>/`、個別プラグインは`plugins/packages/<plugin-id>/<version>/`の構成を使用し、ディレクトリ名と内容のバージョンを一致させる。更新対象の旧配布物は置き換え、READMEのダウンロードリンクやプラグインカタログも配置内容と一致させる。
- バックアップや一時ファイルが必要な場合は、この公開用リポジトリの外に置く。
