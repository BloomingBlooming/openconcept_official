{
    "schema_version": 1,
    "id": "database-postgresql-adapter",
    "name": "PostgreSQL Database Adapter",
    "version": "1.5.1",
    "enabled": false,
    "description": "Connects to an administrator-provisioned PostgreSQL database, migrates the canonical data, and exposes credential-free capabilities.",
    "entry": "plugin.php",
    "assets": {
        "styles": ["assets/plugin.css"],
        "scripts": ["assets/plugin.js"]
    },
    "ui": {
        "sidebar": {
            "label": "PostgreSQL Adapter",
            "icon": "DB",
            "placement": "default"
        }
    }
}
